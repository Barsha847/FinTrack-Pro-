/**
 * FinTrack Pro - Navigation Module
 * (Authentication form handlers are managed inside validation.js)
 */

export function initNavigation() {
  // Handled in validation.js to prevent duplicate submission event registration.
}
